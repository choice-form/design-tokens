{
  "name": "@choiceform/design-tokens-workspace",
  "version": "0.1.0",
  "description": "Design tokens system workspace",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter examples dev",
    "dev:tokens:js": "pnpm --filter @choiceform/design-js-tokens dev",
    "dev:tokens:scss": "pnpm --filter @choiceform/design-scss-tokens dev",
    "type-check": "pnpm -r type-check",
    "lint:css": "pnpm --filter @choiceform/design-js-tokens lint:css",
    "lint:css:fix": "pnpm --filter @choiceform/design-js-tokens lint:css:fix",
    "clean": "pnpm -r exec rm -rf dist node_modules",
    "dev:examples": "pnpm --filter @choiceform/design-tokens-examples dev",
    "build:examples": "pnpm --filter @choiceform/design-tokens-examples build"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
